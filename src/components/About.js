import React from "react";
import "./About.css";

const About = () => {
  return (
    <div className="abt-pg">
      <h2>About</h2>
      <p>
        The folder structure of the project:
        <ul>
          <li>Src has a folder called as components and services</li>
          <li>
            The component folder consists of login, productlist, about files
            with css
          </li>
          <li>
            The services folder has a file called as authService in which all
            the api's have been implemented
          </li>
        </ul>
        <hr/>
        Process to start the project:
        <ul>
          <li>Src has a folder called as components and services</li>
          
        </ul>
      </p>
    </div>
  );
};

export default About;
